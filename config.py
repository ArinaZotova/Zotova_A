# config.py

"""
Конфигурационный файл проекта динамической маршрутизации.
Здесь задаются параметры территории, модели трафика и экспериментальных сценариев.
"""

# Территория исследования.
# Берем более крупный участок, чтобы у алгоритма были альтернативные маршруты.
PLACE_NAME = "Central Administrative Okrug, Moscow, Russia"

# Тип дорожной сети для OSMnx.
NETWORK_TYPE = "drive"

# Времена отправления в минутах от начала суток.
DEPARTURE_TIMES = {
    "night_02_00": 120,
    "morning_peak_08_00": 480,
    "day_13_00": 780,
    "evening_peak_18_00": 1080,
}

# Параметры утреннего и вечернего пиков.
MORNING_PEAK_CENTER = 8 * 60
EVENING_PEAK_CENTER = 18 * 60

MORNING_PEAK_SIGMA = 70
EVENING_PEAK_SIGMA = 80

# Минимальная расчетная скорость, км/ч.
MIN_SPEED_KPH = 5.0

# Базовые скорости по категориям дорог, км/ч.
DEFAULT_SPEEDS_KPH = {
    "motorway": 90,
    "trunk": 80,
    "primary": 60,
    "secondary": 50,
    "tertiary": 45,
    "residential": 30,
    "living_street": 20,
    "service": 20,
    "unclassified": 30,
}

# Базовая глубина снижения скорости в часы пик по категориям дорог.
# Первое число — утренний пик, второе — вечерний пик.
TRAFFIC_DROP_BY_HIGHWAY = {
    "motorway": (0.35, 0.40),
    "trunk": (0.40, 0.45),
    "primary": (0.35, 0.40),
    "secondary": (0.30, 0.35),
    "tertiary": (0.25, 0.30),
    "residential": (0.15, 0.20),
    "living_street": (0.10, 0.12),
    "service": (0.10, 0.12),
    "unclassified": (0.18, 0.22),
}

# Обычная зона повышенной загруженности.
# Она используется в основной модели трафика всегда,
# независимо от галочки в веб-интерфейсе.
#
# Формат:
# min_lat, max_lat, min_lon, max_lon.
CONGESTION_ZONE = {
    "min_lat": 55.745,
    "max_lat": 55.770,
    "min_lon": 37.585,
    "max_lon": 37.635,
}

# Насколько дополнительно снижается скорость в обычной зоне перегрузки.
# 0.35 означает дополнительное снижение на 35% в центре пика.
CONGESTION_ZONE_EXTRA_DROP_MORNING = 0.35
CONGESTION_ZONE_EXTRA_DROP_EVENING = 0.40

# Стартовая и конечная точки для экспериментального режима main.py.
# Формат: (широта, долгота)
START_POINT = (55.7498, 37.5397)   # район Москва-Сити
END_POINT = (55.7576, 37.6606)     # район Курского вокзала

# Папки для результатов.
RESULTS_DIR = "results"
MAPS_DIR = "maps"
FIGURES_DIR = "figures"


# ------------------------------------------------------------
# Дополнительный демонстрационный сценарий:
# модельное локальное ухудшение дорожной ситуации.
#
# Это НЕ реальные данные о ДТП.
# Это искусственный сценарий, который показывает, что модель
# может учитывать локальное временное снижение скорости.
#
# В веб-интерфейсе этот сценарий включается галочкой.
# ------------------------------------------------------------

# Значение по умолчанию.
# В веб-интерфейсе пользователь может включить или выключить этот режим.
ENABLE_INCIDENT_SCENARIO = False

# Отдельная прямоугольная зона модельного локального ухудшения.
# Это не вся центральная зона, а более конкретный участок,
# который можно визуально показать на карте.
#
# Формат:
# min_lat, max_lat, min_lon, max_lon.
#
# Начальные координаты подобраны для центральной части маршрута
# Москва-Сити -> Курский вокзал. Потом при необходимости подберем точнее.
INCIDENT_ZONE = {
    "min_lat": 55.748,
    "max_lat": 55.762,
    "min_lon": 37.595,
    "max_lon": 37.625,
}

# Время максимального влияния локального ухудшения.
# 18:00 = 1080 минут от начала суток.
INCIDENT_CENTER_TIME = 18 * 60

# Ширина временного влияния.
# Чем больше значение, тем дольше действует ухудшение.
INCIDENT_SIGMA = 35

# Максимальное дополнительное снижение скорости.
# 0.60 означает дополнительное снижение скорости на 60%
# в центре временного влияния.
INCIDENT_EXTRA_DROP = 0.60
